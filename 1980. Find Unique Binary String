function findDifferentBinaryString(nums: string[]): string {

    const n = nums.length;

    const set = new Set(nums.map(e => parseInt(e,2)));

    for(let i = 0; i <= n; i++){
        if(!set.has(i)) return i.toString(2).padStart(n,"0")
    }
};
